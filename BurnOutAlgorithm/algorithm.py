def count_burnout_chance(*, suppose_commits, suppose_tasks, suppose_messages, suppose_crunches):
    print(suppose_commits)
    print(suppose_tasks)
    print(suppose_messages)
    print(suppose_crunches)

    commit = 0.3

    commits = [suppose_commits]
    tasks = [suppose_tasks]
    messages = [suppose_messages]
    crunches = [suppose_crunches]
    peoples_ids = []

    #
    #for id in peoples_ids:
    # commit * 0.3 + tasks * 0.2 + ... = 0.8
    #
    #

    print(peoples_ids)
    # burn_out_flags = []
    # sets = [suppose_commits, suppose_tasks, suppose_messages, suppose_crunches]
    # # Итерируем по словарям
    # for data_set in sets:
    #     # Предполагая, что 'user_id' — это единственный элемент во множестве
    #     user_id = next(iter(data_set), None)
    #
    #     # Если 'user_id' присутствует, добавляем его в список
    #     if user_id is not None:
    #         peoples_ids.append(user_id)
    #
    #     # Далее вы можете использовать peoples_ids по вашему усмотрению
    # print(peoples_ids)

    # ГИГА НИГА ФОРМУЛА

